import 'package:flutter/material.dart';

// Brand Colors
const Color primaryColor = Color(0xFF7eee00); // Bright green
const Color backgroundColor = Color(0xFF354247); // Dark slate
const Color accentColor = Color(0xFF354247); // Dark slate
const Color infoColor = Color(0xFF7EEE00); // Bright green
const Color titleColor = Color(0xFF4CAF50); // Material green
const Color destructiveColor =
    Color(0xFFE53935); // Material red for delete/remove actions

// Text Colors
const Color textColor = Color(0xFFE0E0E0); // Light gray for general text
const Color textColorSecondary = Color(0xFFB0B0B0); // Dimmer gray for subtitles

// UI Element Colors
const Color progressBarColor = Colors.cyan;
const Color darkBackgroundColor = Color.fromARGB(255, 19, 19, 19);

// Collaborator Status Colors
const Color mutualStatusColor =
    Color(0xFF7EEE00); // Same as primary for mutual connections
const Color iHelpStatusColor = Color(0xFF00BCD4); // Cyan for outgoing help
const Color helpsMeStatusColor =
    Color(0xFF8BC34A); // Light green for incoming help
const Color pendingStatusColor = Color(0xFFFFB74D); // Orange for pending
const Color searchBarBackground =
    Color(0xFF2A363B); // Slightly lighter than background
const Color dropdownBackground = Color(0xFF2A363B); // Matching search bar

// Control Colors
const Color controlTextColor = Color(0xFFE0E0E0); // Light gray for control text
const Color controlLabelColor = Color(0xFFCCCCCC); // Slightly dimmer for labels
const Color switchActiveColor =
    Color(0xFF7eee00); // Match primary for active switch
const Color switchTrackColor =
    Color(0xFF4A5A61); // Lighter than background for track
const Color dropdownButtonColor =
    Color(0xFF2A363B); // Match searchBarBackground

// Button Colors
const Color buttonIconColor = textColor; // White color for button icons

const Color primaryTextColor = Color(0xFF000000); // or your desired color
const Color secondaryTextColor = Color(0xFF666666); // or your desired color
